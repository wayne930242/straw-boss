# straw-boss

Run the suite with `uv run --with pytest pytest -q tests`.
Write skills, `docs/`, `README.md`, and `CONTEXT.md` in English, including quoted user replies; `tests/test_skill_instruction_quality.py` enforces it, and `README.zh-TW.md` is the one Chinese surface.
