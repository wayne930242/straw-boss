## 1. Scripts and instruction-file schema

- [x] 1.1 `scripts/dispatch-task.py`: `write_instruction` gains an optional `--main-agent-pane-id` (recorded as `main_agent_herdr_pane_id`, `None` when omitted) and always records `main_agent_send_message_peer` (fixed value `"straw-boss-orchestrator"`, matching the existing `/rename` convention) on the instruction payload.
- [x] 1.2 New `scripts/get-main-agent.py`: takes `--instruction-path <path>`, reads the instruction file, prints `{"main_agent_herdr_pane_id": ..., "main_agent_send_message_peer": ...}`. Errors clearly if the file doesn't exist or neither field is populated.
- [x] 1.3 New `scripts/report-progress.py`: takes `--instruction-path <path> --note "<text>"`, appends a `{"timestamp": ..., "note": ...}` line to a sibling `<instruction-path stem>.progress.jsonl` (create the file on first call). Callable any number of times; never overwrites; never touches the instruction file itself or sends any message.
- [x] 1.4 `scripts/report-task-status.py`: add `--instruction-path <path>` as an alternative to `--plan`/`--task` (mutually exclusive with them — reject if both or neither given). When given an instruction path, read it: if it carries `plan_id`/`task_id`, write to the existing `~/.straw-boss/plans/<slug>/status/<task_id>.json` location (unchanged location/format); otherwise write to a sibling `<instruction-path stem>.status.json`. `VALID_STATUSES` stays exactly as-is — `progress` is deliberately not a status value here; progress notes go through `report-progress.py` (1.3) instead, to avoid the `Monitor`-dedup-by-filename hazard described in `design.md`'s Decisions. Smoke-tested (1.1-1.4 together) against a throwaway `$HOME` — all four scripts and the plan-vs-standalone resolution/mutual-exclusivity checks confirmed working.
- [x] 1.5 `skills/dispatching-work/references/dispatch-mechanics.md`: update the "Instruction file" schema block with the two new fields (`main_agent_herdr_pane_id`, `main_agent_send_message_peer`), and document all four scripts from 1.1-1.4 (`get-main-agent.py`, `report-progress.py`, `report-task-status.py`'s new `--instruction-path` form) alongside the existing script docs.

## 2. Shared coordination sources (single source of truth)

- [x] 2.1 `skills/notifying-main-agent/SKILL.md`: restructure into two branches — "Branch: Ask an informational question" (existing Task 1-3 content, unchanged) and a new "Branch: Report your own status" (entry condition: reached `done`/`failed`/a checkpoint the main agent must act on; look up reachability via `get-main-agent.py` per `design.md`'s Decisions, not from prompt recall; requires `SendMessage`, a herdr nudge is optional/additive only; message shape per `design.md`'s Decisions). Note `report-progress.py` as the separate any-time channel for non-terminal updates (no `SendMessage` involved). Update the frontmatter `description` to cover both branches. Task 3's never-authorization rule applies to both branches.
- [x] 2.2 `skills/dispatching-work/references/cross-session-coordination.md`: extend "What the dispatch instruction states, per mode" to add the reporting-obligation sentence per mode, stating the report specifically requires `SendMessage` (herdr nudge optional) and that reachability is looked up via `get-main-agent.py`, not recalled from prose.
- [x] 2.3 `skills/dispatching-work/references/plan-mechanics.md`: "Reporting status" section — add that a plan task also sends the `SendMessage` push in addition to its unchanged status-file write, and MAY call `report-progress.py` at any point beforehand. "Monitoring completion" section — reframe `Monitor`'s polling loop as the fallback, not the primary notice. "User-clarification checkpoints" section — add the escalation-order step (try a stronger second opinion, if available, before `awaiting-user-input`, for genuine technical difficulty as distinct from missing context or a judgment call).
- [x] 2.4 `skills/dispatching-work/SKILL.md`: update the "Three checkpoint types" table and Red Flags to describe the push-primary/pull-fallback contract; add a pointer to the escalation-order addition in `plan-mechanics.md` and to `report-progress.py` for progress visibility.

## 3. Specialist skills (dispatch-instruction assembly)

- [x] 3.1 `skills/shipping-task/SKILL.md`: Task 4 — update instruction-assembly wording to state the reporting obligation (not just the reachability pointer), and to mention `report-progress.py` as available throughout the task. Task 5 — reframe checkpoint detection as push-first, pull as fallback.
- [x] 3.2 `skills/inspecting-app/SKILL.md`: add the `notifying-main-agent` pointer and reporting obligation to its "Dispatch:" bullet, matching `shipping-task`'s pattern.
- [x] 3.3 `skills/investigating-app/SKILL.md`: same as 3.2.
- [x] 3.4 `skills/troubleshooting-app/SKILL.md`: update its existing brief `notifying-main-agent` mention to the new push-obligation contract.

## 4. `peeking-work` reads the progress trail first

- [x] 4.1 `skills/peeking-work/SKILL.md`: before reading a dispatch's live pane/transcript, read its `<instruction-path stem>.progress.jsonl` tail (and, for a plan task, its status file) first; only fall back to a live pane/transcript read when the trail doesn't answer the question. Update its Overview/Task description and any Red Flag that currently assumes a live read is the only source.

## 5. `init` / `create-great-harness` (reverse the existing pull-only mandate)

- [x] 5.1 `skills/init/SKILL.md` Task 9: replace "Detect completion mechanically, not by waiting on a courtesy message" with the push-primary/pull-fallback description (point at `cross-session-coordination.md`/`notifying-main-agent` rather than restating); update the matching Red Flag entry (currently: "Wait for the dispatched agent's `notifying-main-agent` message to know it's done" — no...).
- [x] 5.2 `skills/create-great-harness/SKILL.md` Task 6: update the "Invoked as a dispatched agent" step to require the `SendMessage` push in addition to the turn's own final output; update both matching Red Flags (currently: "Don't additionally push it through `notifying-main-agent`..." and "Push the final report through `notifying-main-agent` too, just to be safe" — no...).

## 6. `boss-say` batch loop

- [x] 6.1 `skills/boss-say/SKILL.md` Task 5/6: note the `SendMessage` push as a faster-than-polling signal for in-flight plan tasks; keep the `Monitor` polling loop as the fallback, unchanged in mechanics.

## 7. Docs

- [x] 7.1 `docs/architecture.md`: update `notifying-main-agent`'s Components-table row to describe both branches (question and report) and the `SendMessage`-required distinction.
- [x] 7.2 `docs/roles.md`: update "Authority over in-flight dispatched work"'s closing sentence ("a dispatched agent's own outcome... still reports through its status file the same way it always has") to describe the push-plus-bookkeeping contract, per `design.md`'s Open Question — update in place, not as a fourth inform/redirect/cancel-style verb (this is the reverse direction: agent → main agent).

## 8. Validation

- [x] 8.1 Run `openspec validate --changes --strict` (or the change-scoped equivalent) and resolve any reported issues.
- [x] 8.2 Grep the repo for any remaining "fire-and-forget with no delivery guarantee" / "never push through `notifying-main-agent`" style language outside the files touched above, and update or reconcile it so no stale contradicting instruction remains. Found and fixed one additional stale claim in `dispatch-mechanics.md`'s `claude-p` section ("There is no plan-style status file for a non-plan dispatch to poll instead — don't go looking for one" — no longer true, and that section didn't yet mention the required `SendMessage` push at all).
- [x] 8.3 Smoke-test the four scripts directly (`dispatch-task.py write` with `--main-agent-pane-id`, `get-main-agent.py`, `report-progress.py`, `report-task-status.py --instruction-path`) against a throwaway instruction file to confirm the schema and file paths actually work before relying on the docs alone. Done (before the doc edits, against a throwaway `$HOME`): all four scripts, both the plan-task and standalone `--instruction-path` resolution paths, and the `--plan`/`--instruction-path` mutual-exclusivity guard all confirmed working.
- [x] 8.4 Post-completion adversarial review (two passes) found and fixed four defects not caught by 8.1/8.2/8.3: (1) `main_agent_send_message_peer` was a fixed literal (`straw-boss-orchestrator`) shared by every main agent — a real `SendMessage` delivery hazard once two main agents run concurrently (`shared-resource-coordination.md` already assumes this can happen); `dispatch-task.py write` now takes `--main-agent-peer-name` for a per-session-unique renamed value, with the literal only as an explicit unsafe fallback. (2) `wrap-up-task.py` archived only the instruction file, orphaning a standalone dispatch's new `.status.json`/`.progress.jsonl` siblings in the live `dispatch/` directory, where `.status.json` would also read as a phantom already-`done` instruction to the List branch's `*.json` scan — `wrap-up-task.py` now archives both siblings alongside the instruction, and the List branch instruction now excludes them explicitly. (3) `report-task-status.py --instruction-path` was only actually named in `create-great-harness`; `shipping-task`/`inspecting-app`/`investigating-app`/`troubleshooting-app` said "the required push" without naming the terminal-state-record script or its `--instruction-path` form — all four now name it explicitly. (4) nothing told the dispatched agent its own instruction path, which `get-main-agent.py`/`report-progress.py`/`report-task-status.py --instruction-path` all require — `cross-session-coordination.md`'s per-mode instruction-content block now states it explicitly (computed before `dispatch-task.py write` is called, since its own `--task` argument is this same prompt). A fifth, smaller gap found alongside these: `wrap-up-task.py`'s non-terminal refusal guard covered only plan tasks, not a standalone dispatch's own `.status.json` — now checked the same way when present (absent is not itself a refusal, for backward compatibility with dispatches that never wrote one). Every fix re-smoke-tested individually (peer-name plumbing, sibling archiving, and the wrap-up non-terminal guard for a standalone dispatch) against a throwaway `$HOME`.
