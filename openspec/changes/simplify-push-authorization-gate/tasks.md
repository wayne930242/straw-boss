## 1. `skills/shipping-task/SKILL.md`

- [x] 1.1 Overview: change "Push (opening an MR/PR) and merge are the mutations the agent cannot self-authorize" to state that only merge and a push to a branch other than the task's own feature branch (root submodule pointer-bump, an app-owned `gitWorkflowSkill`'s protected-branch release push) need authorization; a push to the task's own feature branch (and further pushes to it) needs none but must still be reported.
- [x] 1.2 Task 3's full-flow fallback steps: replace "stop before pushing (Task 5): push the branch and open an MR/PR once authorized" with the agent pushing and opening the MR/PR on its own feature branch on its own, reporting it (branch + MR/PR reference) via the required non-blocking `SendMessage` push, and continuing (addressing review feedback, further pushes to the same branch) — only stopping before merge. Leave the existing monorepo-root submodule pointer-bump line (Task 5, currently "that's a separate mutation — ask about it separately") untouched — that push targets the root's own branch, not this task's feature branch, so it stays gated; do not let it read as folded into the new exemption.
- [x] 1.2b Task 3's `gitWorkflowSkill`-owned path: the sentence "the dispatch instruction... tells the agent to run that skill's remaining steps (commit, MR/release mechanics) to completion" must itself carry the feature-branch scope — "to completion" as currently worded would otherwise absorb a release/version-bump push to a protected branch into the exemption. Add the scope directly to this sentence, not only to the fallback steps (1.2) or Task 4's assembled-instruction wording (1.3).
- [x] 1.3 Task 4 (dispatch-instruction assembly): update the stop-before-mutation instruction text so it names merge, plus any push landing outside the task's own feature branch, as the two things still gated — and add distinctly-worded instruction text for the feature-branch push notification (report-and-continue, never stop-and-wait) — see design.md's "Dispatch-instruction wording" and "The exemption is scoped..." decisions for why the wording must carry the scope explicitly, not just say "push."
- [x] 1.4 Task 4's Verification line: update "every dispatch's instruction states..." to also require the push-notification wording be present, scoped to the feature branch, and distinguishable from the merge/other-branch-push stop-and-wait wording.
- [x] 1.5 Task 5: retitle/narrow to merge authorization (plus any push outside the task's own feature branch — the existing submodule-pointer-bump line stays here, unchanged); remove the feature-branch push/MR authorization step. Add a new branch for the push-FYI notification: relay the branch/MR reference to the user, no authorization obtained, no session resumed — non-blocking, distinct from the `awaiting-authorization` (blocking, this skill acts) and `awaiting-user-input` (not this skill's job) branches already documented there.
- [x] 1.6 Task 5's Verification line ("every push/merge the agent performed was preceded by an explicit authorization"): narrow to merge and any push outside the task's own feature branch; the feature-branch push itself needs none.
- [x] 1.7 Red Flags: update "Commit doesn't need authorization anymore, so push/MR probably doesn't either" (now accurate for the feature branch specifically — remove or invert, but keep it clear that a push to another branch, e.g. the submodule pointer-bump, still needs it), the line distinguishing push-authorization vs `awaiting-user-input` (now three things to distinguish: feature-branch push-FYI, merge/other-branch-push authorization, `awaiting-user-input`), and add a new Red Flag for "the agent stopped and waited after a feature-branch push out of habit" / "the main agent tried to obtain authorization for a feature-branch push notification" / "a submodule pointer-bump or app-owned release push got waved through as if it were the now-exempt feature-branch push."

## 2. `docs/roles.md`

- [x] 2.1 "Autonomy boundary" section: narrow "It may not silently authorize a push/merge... those gates are absolute" to merge, plus any push outside the task's own feature branch — a feature-branch push is no longer a gate this autonomy could ever have been used to bypass; a push to another tracked branch stays exactly as absolute as before.

## 3. `.claude-plugin/plugin.json`

- [x] 3.1 The description's "an authorization gate on every push/merge" claim is now inaccurate for a feature-branch push. Narrow it to merge (and any push outside the task's own feature branch) — same scoping as everywhere else in this change, one-line edit.

## 4. Spec consistency

- [x] 4.1 Run `openspec validate --strict` against the change and confirm both spec deltas (`dispatch-authority`, `dispatch-completion-reporting`) apply cleanly. Confirmed: "Change 'simplify-push-authorization-gate' is valid".
- [x] 4.2 Cross-check every file listed in `proposal.md`'s Impact section was actually touched by tasks 1-3: `skills/shipping-task/SKILL.md` ✓ (group 1), `docs/roles.md` ✓ (2.1), `.claude-plugin/plugin.json` ✓ (3.1); the two spec-delta files were already authored during planning and validate cleanly (4.1) — nothing further to touch on them at apply time, same pattern as the first change in this project.

## 5. Consistency read-through (no new script or CLI interaction this time — no live-session verification needed, per the proposal's own scoping)

- [x] 5.1 Re-read the full `shipping-task/SKILL.md` after editing, end to end. Found and fixed one stale reference the earlier tasks didn't cover: Overview line 8 pointed to "the push/merge authorization gate below" (unscoped) — retitled to "the merge/other-branch-push authorization gate below". Confirmed: no remaining reference implies a feature-branch push still needs authorization, and none implies a push to another branch (submodule pointer-bump, app-owned release push) no longer needs it. Task 5's four checkpoint-response branches (`awaiting-authorization`, feature-branch push-FYI, `awaiting-user-input`, `awaiting-main-agent`) are each clearly distinguished from the other three.
- [x] 5.2 Walked through a concrete dispatch instruction in prose: "...pushing your own feature branch needs no authorization — once pushed, send the required SendMessage push (branch + MR/PR reference) and keep working, do not wait. Stop and wait for authorization only for: (a) merging this branch into <base>, and (b) any push landing on a branch other than your own feature branch (e.g. a monorepo-root submodule pointer-bump, or your release tooling's push to a protected branch)." The two clauses use different verbs ("keep working" vs "stop and wait") tied to explicitly named triggers, and the submodule case is named as an explicit example of (b) rather than left for the reader to infer — unambiguous, no further pass needed.

## 6. Repo-wide consistency sweep (found via advisor review after the initial 5 groups were applied — not in the original proposal's Impact list)

A repo-wide grep for `push/merge`, `push or merge`, `ready to push`, and `awaiting-authorization` after group 1-5 turned up real hits the original Impact list missed, several load-bearing for agent behavior rather than merely descriptive.

- [x] 6.1 `skills/i-am-orchestrator/SKILL.md` — the SessionStart priming text's `awaiting-authorization` bullet (most-read statement of this rule in the plugin, injected at every main-agent session start) scoped to merge/other-branch-push; also generalized its stale "Four checkpoint/report types" table citation (already out of date pre-existing, now more so after 6.2's rename) to not hardcode a count.
- [x] 6.2 `skills/dispatching-work/SKILL.md` — the checkpoint/report types table: scoped the `awaiting-authorization` row, added a new row for the feature-branch push-FYI, renamed the table "Five" → "Six," fixed the "push/merge readiness gate" phrase in the escalation-order paragraph.
- [x] 6.3 `skills/dispatching-work/references/plan-mechanics.md` — the "Authorization checkpoints" section (this is the doc `shipping-task` Task 5 cites by name; leaving it stale would have made `shipping-task` cite a contradicting doc), its JSON example note, and the "User-clarification checkpoints" section's "push/merge decision" phrase.
- [x] 6.4 `skills/dispatching-work/references/dispatch-mechanics.md` — the `claude-p` checkpoint-detection paragraphs assumed a push always stopped the agent; fixed to reflect that a feature-branch push happens within the same one-shot turn without stopping it, only merge/other-branch-push do.
- [x] 6.5 `skills/notifying-main-agent/SKILL.md` — real gap, not just stale wording: no existing branch let a worker correctly report a push-FYI (the "done/failed/checkpoint" branch mandates a status-record write with an enumerated value and frames every case as a stop). Added "Branch: Report a completed push, then continue" as a third top-level branch (skips the status write, sends the SendMessage push, explicitly does not wait); updated frontmatter, the "Report own status" entry condition, Task 3's heading and authorization-safety wording, and Red Flags.
- [x] 6.6 `docs/architecture.md` — the `notifying-main-agent`/`shipping-task` table rows, the routing-rule step 6, and the "External mutations stay gated" section (the paragraph most directly describing this exact rule to a human reader).
- [x] 6.7 `README.md`, `README.zh-TW.md` — the `shipping-task` one-line table description, English and Traditional Chinese.
- [x] 6.8 `CONTEXT.md` — the "Autonomy boundary" duplicate of `docs/roles.md`'s text, scoped identically. (Note, out of scope for this change: `CONTEXT.md` was already stale independent of this change — still describes "three" main-agent authority actions and lacks the first change's `Resolve` action; flagged to the user, not fixed here.)
- [x] 6.9 `openspec/specs/dispatched-agent-escalation/spec.md` — not in the original Modified Capabilities list; added a new MODIFIED delta (`specs/dispatched-agent-escalation/spec.md` in this change) narrowing "the push/merge authorization gate" to merge/other-branch-push, since escalation order references the same gate this change redefines.
- [x] 6.10 Re-ran `openspec validate --strict` after all of the above.
- [x] 6.11 Grepped repo-wide for `checkpoint/report types` to check the "Five" → "Six" table rename (6.2) didn't strand another citation. Found and fixed one: `dispatching-work/SKILL.md`'s own Red Flags section still said "per 'Four checkpoint/report types' above" (already stale pre-existing since the first change made it "Five"; now fixed to "Six" along with the rest of this change's rename).
- [x] 6.12 Version bump: `.claude-plugin/plugin.json` 0.14.0 → 0.15.0 — this change adds a feature (a new `notifying-main-agent` branch, a new checkpoint-table row), not just a fix, consistent with how the first change (`add-awaiting-main-agent-checkpoint`) bumped for the same reason.

## 7. `boss-say/SKILL.md` — the batch path's own push-FYI handling (found via a second advisor pass; the `awaiting-authorization` grep in group 6 used `push and merge` instead of `push/merge`, so it never ran and missed this)

A dispatched batch item's full-flow git lifecycle is driven by `boss-say`'s own Task 5, not by `shipping-task` (batch items never go through `shipping-task` — `boss-say` reuses `dispatching-work`'s mechanics directly, per its Task 3/16). Without an explicit step, the batch loop had no branch for a feature-branch push notification at all — it would either be mistaken for `awaiting-authorization` (the exact anti-pattern already closed on the standalone side) or simply not recognized, since it arrives only as a raw `SendMessage`, never a `read-plan-status.py`/`Monitor`-visible status value.

- [x] 7.1 Added Task 5 step 7 ("A feature-branch push notification is not a plan-status event at all"): relay to the user as an FYI, no authorization, no slot held or freed, no `Monitor` fallback (same accepted limitation as the informational-question channel). Renumbered the old step 7 (idle in-flight peeking) to step 8.
- [x] 7.2 Fixed every existing cross-reference to the old step numbering broken by the renumber: line 68 ("Task 5 step 7's stalled-batch detection"), Task 6's "One-shot batch" and "Self-paced batch, on a tick" paragraphs, and three Red Flags — all now say step 8 where they meant the idle-peeking step, and reference the new step 7 where relevant. Added a new Red Flag for treating a feature-branch push as `awaiting-authorization`.
- [x] 7.3 Updated Task 5's Verification line and Task 6's "One-shot batch" paragraph to state the push-FYI's `SendMessage`-only, no-`Monitor`-coverage delivery path explicitly.
- [x] 7.4 Re-ran `openspec validate --strict` after this group. Re-grepped `boss-say/SKILL.md` for `step 6\|step 7\|step 8` to confirm every cross-reference now points at the right step.
