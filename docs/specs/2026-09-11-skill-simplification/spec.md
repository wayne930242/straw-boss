Status: approved
Approved at: 2026-09-11
Approved from: User approved all recommendations in the preceding skill audit.

1. Each skill owns a distinct responsibility. Planning belongs to `boss-say`, app resolution to `work-on`, brief construction and dispatch cleanup to `dispatching-work`, and graph/anchor/review policy to `choosing-graph`.
2. Initialization changes only requested or missing configuration, reuses confirmed choices, and checks readiness before any dispatch. Direct work may proceed with the target instructions loaded.
3. Batch planning asks only unresolved user choices. Persisted events drive scheduling; a peek answers a user question or an evidence/state discrepancy.
4. Skill prose carries actionable conditions and completion criteria. Shared rules use named links; numbered cross-skill references and incident narratives are removed.
5. Existing identity validation, terminal/checkpoint distinction, same-task continuation, dependency gating, one review per change-set, resource release, and evidence-backed recovery remain reachable from every applicable route.
6. Existing skill names and executable interfaces remain compatible. Tests follow the rule owner and links rather than requiring duplicate prose.

Reality anchor: a scenario review of the edited instruction paths, local link validation, and the instruction/lifecycle regression suite before completion. No installation or live dispatch is required for this documentation change.
